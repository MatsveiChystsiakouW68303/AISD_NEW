def posortowanie(lista, n):
    i = 0
    while i < len(lista) and n > lista[i]:
        i += 1
    lista.insert(i, n)
list = [1, 3, 5, 7, 9]
n=int(input("Jaka liczba będzie do posortowania: "))
posortowanie(list, n)
print(list)
