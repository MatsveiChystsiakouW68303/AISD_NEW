def is_balanced(str):
    stack = []
    for char in str:
        if char == "(":
            stack.append(char)
        elif char == ")":
            if len(stack) == 0:
                return False
            stack.pop()
    return len(stack) == 0
print(is_balanced('((()))'))
